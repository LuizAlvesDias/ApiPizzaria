info:
  name: 03 Cadastrar pizza
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/pizzas"
  body:
    type: json
    data: |-
      {
        "nome": "Portuguesa",
        "sabor": "Presunto, queijo, ovo, cebola e azeitona",
        "tamanho": "Grande",
        "preco": 52.90
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
