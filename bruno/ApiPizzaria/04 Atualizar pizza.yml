info:
  name: 04 Atualizar pizza
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/pizzas/3"
  body:
    type: json
    data: |-
      {
        "nome": "Portuguesa Especial",
        "sabor": "Presunto, queijo, ovo, cebola, azeitona e bacon",
        "tamanho": "Grande",
        "preco": 59.90
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
